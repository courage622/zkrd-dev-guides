# 中科融道 GitLab MR 代码审查操作规范

## 1. 目的和适用范围

本规范用于统一 Merge Request（以下简称 MR）创建后的代码审查、意见反馈、修改、复审和合并操作。

适用于以下 MR：

- `feature/* → dev`
- `fix/* → dev`
- `release/* → main`
- `hotfix/* → main`，以及按规定需要同步回 `dev` 的 MR

代码审查的目的不是检查开发者是否“写得和评审人一样”，而是尽早发现功能错误、安全风险、兼容性问题、遗漏场景和难以维护的代码，并保证重要修改经过其他人员确认后再合并。

## 2. 角色和职责

### 2.1 MR 作者

MR 作者通常是该分支的主要开发者，负责：

- 创建 MR 并填写完整说明。
- 指定合适的 Reviewer。
- 确保 MR 只包含当前任务相关变更。
- 逐条回复和处理代码审查意见。
- 在原分支修改、测试、提交并推送代码。
- 修改完成后重新请求评审。

### 2.2 Reviewer

Reviewer 是代码评审人，负责：

- 理解任务目标和本次修改范围。
- 检查 MR 的文件和代码变更。
- 在具体代码行或 MR 中提出清晰、可执行的意见。
- 区分必须修改项、建议修改项和疑问。
- 对作者修改后的代码进行复审。
- 确认所有阻止合并的问题已经解决后批准 MR。

### 2.3 Maintainer

Maintainer 是最终合并责任人，负责：

- 检查 MR 是否满足合并条件。
- 确认审批和讨论处理情况。
- 将 MR 合并到目标分支。
- 合并后删除不再使用的源分支。

MR 作者不得作为自己 MR 的唯一评审人。普通 MR 至少由 1 名熟悉相关模块的人员评审；数据库、权限、安全、公共组件等高风险修改，应增加对应负责人参与评审。

## 3. MR 进入审查前的要求

MR 作者应先完成自查，并在 MR 中填写：

- 任务来源或内部任务编号。
- 修改目的和主要内容。
- 源分支和目标分支。
- 已完成的测试及结果。
- 数据库、配置、接口和部署影响。
- 已知风险及回滚方式。

以下情况不应直接提交正式审查：

- MR 仍处于开发中，却未标记为 Draft。
- 目标分支选择错误。
- 包含与当前任务无关的文件或格式化变更。
- 存在未解决的代码冲突。
- 作者尚未进行基本测试。
- MR 描述缺失，评审人无法判断修改目的。

## 4. 评审人在 GitLab 上的代码审查流程

### 4.1 找到待评审 MR

评审人可以通过以下入口找到 MR：

```text
GitLab 顶部 Review requested
```

或：

```text
项目 → Code → Merge requests
```

打开 MR 后，先确认自己已被设置为 **Reviewer**。

### 4.2 检查 MR 基本信息

在 MR 的 **Overview** 页面检查：

- 标题是否能说明修改目的。
- 任务来源和验收标准是否明确。
- 源分支和目标分支是否正确。
- 修改内容、测试结果、风险和回滚方式是否完整。
- MR 当前是否为 Draft。
- 是否存在代码冲突。
- Commit 数量和说明是否合理。

如果这些信息不足，应先在 MR 中要求作者补充，再进行详细审查。

### 4.3 在 Changes 页面审查变更

进入 MR 的 **Changes** 页面。GitLab 会显示源分支相对于目标分支的文件和代码差异：

- 绿色内容通常表示新增代码。
- 红色内容通常表示删除代码。
- 同一位置同时出现红色和绿色内容，通常表示代码被修改。

评审人应逐文件检查本次变更，但不需要对每一行都发表评论。只在发现问题、存在疑问或需要说明时，针对具体代码行发起讨论。

重点检查：

- 功能实现是否符合任务要求和验收标准。
- 是否遗漏正常流程、边界条件和异常情况。
- 修改范围是否合理，是否误改或漏改文件。
- 是否可能影响已有功能、接口或数据兼容性。
- 是否存在明文密码、密钥、个人信息或敏感日志。
- 权限判断、输入校验和异常处理是否合理。
- 数据库、配置、接口和部署影响是否已说明。
- 代码结构、命名、重复逻辑和可维护性是否合理。
- 作者填写的测试内容是否覆盖主要风险。

Changes 页面主要显示本次 MR 的变更。必要时，评审人还应打开相关的原有代码，了解调用关系和上下文，不能只孤立地检查几行 Diff。

### 4.4 在具体代码行反馈意见

发现问题时，点击代码行旁的评论图标，在对应代码行上留言。

一条讨论原则上只描述一个问题，并使用以下分类：

- `[必须修改]`：功能错误、安全风险、明显缺陷或违反项目强制规范，解决前不能合并。
- `[建议修改]`：能够改善可读性、结构或性能，但不一定阻止本次合并。
- `[疑问]`：评审人需要了解设计目的、业务规则或实现原因。

规范示例：

```text
[必须修改] 此处未处理 user 为空的情况，调用 getName() 时可能出现空指针异常。
建议在调用前进行非空判断，并补充空用户场景的测试。
```

不规范示例：

```text
这里不对。
```

评审意见应尽量说明：

1. 问题是什么。
2. 可能造成什么影响。
3. 建议如何处理。

有明确替代代码时，可以使用 GitLab 的 **Suggestion** 功能给出代码建议。作者确认后可以在 GitLab 网页中应用该建议，GitLab 会将修改提交到 MR 的源分支。

### 4.5 提交本轮评审

GitLab 通常提供两种评论方式：

- **Add comment now**：立即发布当前评论，适合只需要反馈少量问题的情况。
- **Start a review**：先暂存当前评论；继续检查时使用 **Add to review**，最后从 **Your review** 统一提交本轮全部意见。

推荐评审人完整检查 MR 后统一提交，避免作者在评审尚未结束时反复修改。

本轮评审完成后，选择适当的结论：

- **Approve**：没有阻止合并的问题，可以进入合并检查。
- **Comment**：提交意见，但暂不批准，也不明确要求修改。
- **Request changes**：存在必须修改的问题，要求作者修改后重新评审。

`Request changes` 的可用性及其强制效果取决于 GitLab 版本和授权等级。如果当前 GitLab 不提供该功能，评审人应保留 `[必须修改]` 讨论为未解决状态，并且不要点击 **Approve**。

## 5. 评审意见如何通知开发者

评审意见发布后，会保存在 MR 的讨论线程中，并关联到对应文件和代码行。GitLab 通常通过以下方式通知 MR 作者：

- GitLab 网页中的 To-Do、通知或 Review requested 提示。
- GitLab 站内 MR 更新提示。
- 用户已启用的 GitLab 邮件通知。
- 公司另外配置的消息通知渠道。

开发者应以 GitLab MR 页面中的讨论记录为最终依据。邮件或其他消息只用于提醒，不能替代在 MR 中查看和回复意见。

评审意见不会自动修改开发者本地代码。除评审人提交了可直接应用的 Suggestion 外，通常仍需开发者在本地原分支修改代码并重新推送。

## 6. 开发者处理审查结果的流程

### 6.1 查看全部评审意见

MR 作者收到通知后，应进入对应 MR，检查：

- **Overview** 中的整体意见。
- **Changes** 中关联到具体代码行的讨论。
- 本轮评审结论是 Comment、Request changes 还是 Approve。
- 是否存在未解决的 Discussion。

作者应逐条处理，不得只修改代码而不回复讨论，也不得直接忽略 `[必须修改]` 意见。

### 6.2 理解和回复意见

处理每条意见时：

- 已理解并同意：回复计划如何修改。
- 不理解：在原讨论线程中询问，不另外创建重复讨论。
- 不同意：说明业务背景、技术依据和影响，与评审人达成一致。
- 本次无法处理：说明原因和后续处理方式，并取得评审人认可。

示例：

```text
收到。本次会增加 user 为空时的判断，并补充对应测试。
```

### 6.3 在原任务分支修改代码

开发者必须继续在创建该 MR 的原源分支上修改，不要为处理评审意见重新创建另一个 MR。

```bash
git switch feature/123-history-search
git pull --ff-only

# 根据评审意见修改并测试代码

git status
git diff
git add <本次修改的文件>
git diff --staged
git commit -m "fix(history): 处理空查询条件"
git push
```

推送到原源分支后，GitLab 会自动把新 Commit 和代码差异更新到现有 MR 中，不需要重新创建 MR。

如果 GitLab 提供了明确且正确的 Suggestion，作者也可以在 MR 网页端应用，但应用前仍应确认建议适合当前业务逻辑，并在应用后完成必要测试。

### 6.4 在讨论中说明处理结果

代码推送后，作者应回到对应讨论线程回复处理结果，例如：

```text
已处理：增加了 user 为空时的判断，并补充了空用户场景测试。
修改已包含在最新 Commit 中，请复审。
```

如果代码位置经过修改，原讨论可能显示为旧版本或 Outdated，但讨论不会因此自动代表问题已经解决，仍需双方确认处理结果。

原则上由提出 `[必须修改]` 意见的评审人复查后关闭讨论。作者不应在没有说明处理结果、没有取得认可的情况下批量关闭讨论。

### 6.5 重新请求评审

全部意见处理完成后，作者应：

1. 确认所有修改已经 Commit 并 Push。
2. 确认已回复每一条必须修改意见。
3. 在 MR 的 **Reviewers** 区域重新请求原 Reviewer 评审。
4. 等待评审人复审，不自行合并。

## 7. 评审人复审流程

收到重新评审通知后，评审人应：

1. 查看作者对每条意见的回复。
2. 查看新增的 Commit 和最新代码差异。
3. 确认每条 `[必须修改]` 是否真正解决。
4. 检查修改是否引入新的问题。
5. 关闭已经解决的讨论。
6. 对仍未解决的问题继续在原讨论中反馈。
7. 所有阻止项解决后点击 **Approve**。

如果作者仅回复“已处理”，但代码实际没有解决问题，评审人不得关闭讨论或批准 MR。

## 8. 合并条件和操作

最终由 Maintainer 检查以下条件：

- MR 不处于 Draft 状态。
- 源分支和目标分支正确。
- 必要的 Reviewer 已经批准。
- 所有 `[必须修改]` 和相关讨论均已解决。
- 测试结果已经填写。
- 不存在代码冲突。
- 数据库、配置和回滚说明完整。

满足条件后，由 Maintainer 点击 **Merge**。任务分支后续不再使用时，应选择删除源分支。

GitLab Free 的 Approval 可能无法作为强制门禁。此时应通过以下方式落实规范：

- 保护 `dev`、`main` 等核心分支。
- 只允许 Maintainer 向核心分支合并。
- Maintainer 在存在未解决讨论时不得合并。
- MR 作者不得自行合并未经他人评审的代码。

## 9. 完整操作流程

```text
作者创建 MR并指定 Reviewer
        ↓
评审人检查 Overview
        ↓
评审人在 Changes 中逐文件检查
        ↓
针对具体代码行发起讨论
        ↓
提交 Comment 或 Request changes
        ↓
作者在 GitLab 查看并回复意见
        ↓
作者在原分支修改、测试、Commit、Push
        ↓
作者在讨论中说明结果并重新请求评审
        ↓
评审人查看最新变更并复审
        ↓
关闭已解决讨论并 Approve
        ↓
Maintainer 检查合并条件并 Merge
        ↓
删除源分支
```

## 10. 禁止和不推荐的做法

- 未看完整个 MR 就直接点击 Approve。
- 只写“有问题”“改一下”，不说明原因和建议。
- 把多个无关问题写在同一个讨论线程中。
- 用个人聊天代替 GitLab MR 中的正式评审记录。
- 作者修改代码后不 Push，却要求评审人复审。
- 作者不回复意见，直接将所有讨论标记为已解决。
- 评审人已经批准，却仍保留未说明的必须修改要求。
- MR 审查期间对共享分支执行强制推送，导致评审记录难以追踪。
- 未经他人审查，由作者直接合并自己的 MR。

## 11. 评审意见示例

### 11.1 必须修改

```text
[必须修改] 查询参数直接拼接到 SQL 中，存在 SQL 注入风险。
请改为参数化查询，并验证非法输入场景。
```

### 11.2 建议修改

```text
[建议修改] 这段数据转换逻辑与 UserMapper 中的实现重复，
建议复用现有方法；如果本次不调整，请说明两者需要独立维护的原因。
```

### 11.3 疑问

```text
[疑问] 这里在查询失败时返回空列表，而不是向上抛出异常，
是业务规则要求，还是为了兼容现有接口？
```

## 12. 官方参考

- [Merge request reviews](https://docs.gitlab.com/user/project/merge_requests/reviews/)
- [Merge request discussions](https://docs.gitlab.com/user/discussions/)
- [Review suggestions](https://docs.gitlab.com/user/project/merge_requests/reviews/suggestions/)
- [Merge request approvals](https://docs.gitlab.com/user/project/merge_requests/approvals/)
